services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: message-be
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
